#! /usr/bin/env python
# -*- coding: encoding -*-


def f(x:return x%2!=0 and x%3 !=0

filter(f,range(2,25))

# 返回给定序列中所有调用f后返回值为true的元素

map(function,seq)
# 为每一个元素依次调用function后将返回值组成一个链表返回

reduce(function,seq) 返回一个单值
def add(x,y):
	return x+y
reduce(add,range(1,11))
第一次传第一，第二个值进去，然后返回x，依次返回值和下一个值去调用方法，最后返回一个单值
































